

import 'package:flutter/material.dart';

class TopBrandView extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(child: Text('        Top Brand Screen\nDevelopment under process'),),
    );
  }
}
